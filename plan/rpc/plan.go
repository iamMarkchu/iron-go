// Code generated by goctl. DO NOT EDIT!
// Source: plan.proto

package main

import (
	"flag"
	"fmt"

	"iron-go/plan/rpc/internal/config"
	"iron-go/plan/rpc/internal/server"
	"iron-go/plan/rpc/internal/svc"
	"iron-go/plan/rpc/plan"

	"github.com/tal-tech/go-zero/core/conf"
	"github.com/tal-tech/go-zero/zrpc"
	"google.golang.org/grpc"
)

var configFile = flag.String("f", "etc/plan.yaml", "the config file")

func main() {
	flag.Parse()

	var c config.Config
	conf.MustLoad(*configFile, &c)
	ctx := svc.NewServiceContext(c)
	srv := server.NewPlanServer(ctx)

	s := zrpc.MustNewServer(c.RpcServerConf, func(grpcServer *grpc.Server) {
		plan.RegisterPlanServer(grpcServer, srv)
	})
	defer s.Stop()

	fmt.Printf("Starting rpc server at %s...\n", c.ListenOn)
	s.Start()
}
